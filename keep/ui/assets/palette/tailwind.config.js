/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["{keep,ftext}/**/*.{html,js,nim}"],
  theme: {
    extend: {},
  },
  plugins: [
    // require('tailwind-children')
  ],
}

