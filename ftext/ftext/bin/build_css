#!/usr/bin/env bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )/.." && pwd )"

# --minify
tailwindcss -i $DIR/render/static_page.css -o $DIR/render/static_page_build.css -c $DIR/render/tailwind.config.js --watch